package Items;

import javax.swing.*;
import java.awt.*;

public class LabelWithItemIcons extends JLabel{
    public LabelWithItemIcons() {
        setBackground(Color.GRAY);
        setPreferredSize(new Dimension(100, 100));
        setOpaque(true);

    }
}