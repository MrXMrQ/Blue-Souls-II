package GUI;

import javax.swing.*;

public class MyFrame extends JFrame {
    public MyFrame() {
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocation(500,500);
        setSize(1080, 480);
        setTitle(" Blue Souls II");
        setVisible(true);
    }
}
